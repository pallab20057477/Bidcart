# Render SPA routing - serve index.html for all routes
# This ensures that React Router can handle client-side routing

# Static assets should be served normally (don't redirect these)
/static/*   /static/:splat   200
/favicon.ico   /favicon.ico   200
/logo192.png   /logo192.png   200
/manifest.json   /manifest.json   200

# API routes should not be redirected
/api/*   /api/:splat   200

# All other routes should serve index.html for React Router
/*   /index.html   200